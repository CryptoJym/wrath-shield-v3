# Streamlit Cloud friendly requirements (minimal runtime deps)

# UI
streamlit==1.28.1
altair>=5.3.0
pandas>=2.2.3

# HTTP client
httpx==0.25.1

# Database driver (optional at runtime; used when DB is reachable)
psycopg2-binary==2.9.11

# xAI (OpenAI-compatible client)
openai==1.54.0
